server:
  port: 9004

spring:
  application:
    name: config-center-client
  cloud:
    config:
      label: master
      name: application
      profile: prod
      uri: http://localhost:9003

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:7001/eureka