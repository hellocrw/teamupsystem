server:
  port: 9003

spring:
  application:
    name: config-center-client
  cloud:
    config:
      label: master
      name: application
      profile: prod
      uri: http://localhost:9002

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:7001/eureka