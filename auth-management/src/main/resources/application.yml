server:
  port: 9001

spring:
  application:
    name: auth-management

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka