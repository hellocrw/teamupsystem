server:
  port: 9003

spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hellocrw/config-center.git
          search-paths:
            - config-center
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka